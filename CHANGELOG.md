# CHANGELOG

本檔案以繁體中文記錄待辦清單 App 的版本與主要變更。

## [0.2.0] - Step 2
發布日期：未指定

### 新增
- 深色模式切換按鈕：在標題列右側新增主題切換按鈕，按鈕上顯示圖示與文字（淺色時顯示「🌙 深色模式」，深色時顯示「☀️ 淺色模式」）。
- 主題偏好儲存：使用者的主題選擇會儲存到 localStorage，重新整理後維持使用者偏好。
- 跟隨系統主題：若使用者從未手動切換，介面會跟隨作業系統或瀏覽器的 prefers-color-scheme 設定，系統主題變動時會自動更新介面（前提是使用者未手動指定主題）。
- 篩選功能：在輸入框下方新增三個篩選按鈕（全部、未完成、已完成），可依篩選顯示對應的項目。
- 篩選下的空提示：在各種篩選下若結果為空，會顯示對應提示文字（例如「目前沒有未完成的項目」）。

### 變更
- 介面與樣式：將主題相關顏色抽換為 CSS 變數，新增深色主題變數（以 .dark-theme 套用），避免在多處寫死色碼。
- 未完成計數：底部「未完成: N 項」顯示改為永遠反映整體資料，不受目前篩選影響。

## [0.1.0] - Step 1
發布日期：未指定

### 新增
- 新增待辦：提供輸入框與新增按鈕，可新增待辦事項（忽略純空白輸入）。
- 勾選完成：每筆待辦左方有勾選框，勾選後文字加上刪除線並淡化表示完成狀態。
- 刪除項目：每筆待辦右方有刪除按鈕，可以刪除該筆項目。
- 未完成計數：底部顯示未完成項目數量，例如「未完成: N 項」。
- 空清單提示：清單為空時顯示提示文字「還沒有任何待辦事項，新增一個吧!」。
- 本地儲存：使用 localStorage 儲存清單資料，重新整理頁面後資料仍然保留。

### 變更
- 無
